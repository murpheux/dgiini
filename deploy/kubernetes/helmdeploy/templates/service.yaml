apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmdeploy.fullname" . }}
  labels:
    {{- include "helmdeploy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmdeploy.selectorLabels" . | nindent 4 }}
