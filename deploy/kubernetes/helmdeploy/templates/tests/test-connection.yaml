apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmdeploy.fullname" . }}-test-connection"
  labels:
{{ include "helmdeploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "helmdeploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
